```ts
function isEmptyString(string): boolean;
```

Defined in: [string-utils.ts:20](https://github.com/kemotx90/ts-utily/blob/560ae9f8573563ca541d9fb01f012e8591e5b7e5/src/utils/string-utils.ts#L20)

The function checks if a string is empty, including cases where the string is undefined or null.

## Parameters

### string

The `string` parameter is of type `string | undefined | null`, which means it can accept a value of type `string`, `undefined`, or `null`.

`string` | `null` | `undefined`

## Returns

`boolean`

The function `isEmpty` returns a boolean value.
