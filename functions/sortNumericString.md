```ts
function sortNumericString(a, b): number;
```

Defined in: [string-utils.ts:135](https://github.com/kemotx90/ts-utily/blob/560ae9f8573563ca541d9fb01f012e8591e5b7e5/src/utils/string-utils.ts#L135)

The function `sortNumericString` compares two numeric strings and returns a number indicating their relative order.

## Parameters

### a

`string`

The parameter "a" is a string representing the first numeric string to be compared.

### b

`string`

The parameter "b" is a string representing the second value to be compared.

## Returns

`number`

a number.
