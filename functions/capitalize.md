```ts
function capitalize(str): string;
```

Defined in: [string-utils.ts:55](https://github.com/kemotx90/ts-utily/blob/560ae9f8573563ca541d9fb01f012e8591e5b7e5/src/utils/string-utils.ts#L55)

The `capitalize` function takes a string as input and returns the same string with the first letter of each word capitalized.

## Parameters

### str

The `str` parameter is a string that may be `undefined` or `null`.

`string` | `null` | `undefined`

## Returns

`string`

The `capitalize` function returns a string.
