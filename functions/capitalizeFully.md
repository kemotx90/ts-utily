```ts
function capitalizeFully(str): string;
```

Defined in: [string-utils.ts:68](https://github.com/kemotx90/ts-utily/blob/560ae9f8573563ca541d9fb01f012e8591e5b7e5/src/utils/string-utils.ts#L68)

The `capitalizeFully` function takes a string as input and returns a new string with each word capitalized.

## Parameters

### str

The `str` parameter is a string that can also be `undefined` or `null`.

`string` | `null` | `undefined`

## Returns

`string`

The function `capitalizeFully` returns a string.
