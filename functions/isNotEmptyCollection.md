```ts
function isNotEmptyCollection<T>(collection): boolean;
```

Defined in: [collection-utils.ts:19](https://github.com/kemotx90/ts-utily/blob/560ae9f8573563ca541d9fb01f012e8591e5b7e5/src/utils/collection-utils.ts#L19)

The function `isNotEmptyCollection` checks if a collection is not empty.

## Type Parameters

### T

`T`

## Parameters

### collection

The `collection` parameter is of type `T[] | undefined | null`. This means it can accept an array of type `T`, or it can be `undefined` or `null`.

`T`[] | `null` | `undefined`

## Returns

`boolean`

The function isNotEmpty is returning a boolean value.
