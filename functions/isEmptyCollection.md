```ts
function isEmptyCollection<T>(collection): boolean;
```

Defined in: [collection-utils.ts:9](https://github.com/kemotx90/ts-utily/blob/560ae9f8573563ca541d9fb01f012e8591e5b7e5/src/utils/collection-utils.ts#L9)

The function `isEmptyCollection` checks if a collection is empty or not.

## Type Parameters

### T

`T`

## Parameters

### collection

The `collection` parameter is a generic type `T[]` which represents an array of elements of type `T`. It can also be `undefined` or `null`.

`T`[] | `null` | `undefined`

## Returns

`boolean`

a boolean value indicating whether the collection is empty or not.
