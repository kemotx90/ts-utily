```ts
function removeWhiteSpace(str): string;
```

Defined in: [string-utils.ts:102](https://github.com/kemotx90/ts-utily/blob/560ae9f8573563ca541d9fb01f012e8591e5b7e5/src/utils/string-utils.ts#L102)

The `removeWhiteSpace` function removes all white spaces from a given string.

## Parameters

### str

The `str` parameter is a string that can be either a valid string value, `undefined`, or `null`.

`string` | `null` | `undefined`

## Returns

`string`

a string with all whitespace characters removed.
