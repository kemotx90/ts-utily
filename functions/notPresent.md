```ts
function notPresent<T>(...args): boolean;
```

Defined in: [common-utils.ts:26](https://github.com/kemotx90/ts-utily/blob/560ae9f8573563ca541d9fb01f012e8591e5b7e5/src/utils/common-utils.ts#L26)

The function `notPresent` checks if any of the arguments passed to it are not present.

## Type Parameters

### T

`T`

## Parameters

### args

...`T`[]

The `args` parameter is a rest parameter of type `T[]`, which means it can accept any number of arguments of type `T`.

## Returns

`boolean`

a boolean value.
