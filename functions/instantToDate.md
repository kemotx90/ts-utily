```ts
function instantToDate(instant?): Date;
```

Defined in: [date-utils.ts:185](https://github.com/kemotx90/ts-utily/blob/560ae9f8573563ca541d9fb01f012e8591e5b7e5/src/utils/date-utils.ts#L185)

Converts the given instant to a Date object.

## Parameters

### instant?

The instant to convert. If not provided, the current date and time is used.

`string` | `Date`

## Returns

`Date`

- The converted Date object.

## Example

```ts
use for convert Instant(like as java object) to javascript date
```
