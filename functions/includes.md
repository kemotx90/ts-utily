```ts
function includes(string1, string2): boolean;
```

Defined in: [string-utils.ts:10](https://github.com/kemotx90/ts-utily/blob/560ae9f8573563ca541d9fb01f012e8591e5b7e5/src/utils/string-utils.ts#L10)

The function checks if a given string contains another given string, ignoring case sensitivity.

## Parameters

### string1

A string that can be undefined or null.

`string` | `null` | `undefined`

### string2

The `string2` parameter is a string that represents the substring that we want to check if it is present in `string1`.

`string` | `null` | `undefined`

## Returns

`boolean`

The function `includes` returns a boolean value.
