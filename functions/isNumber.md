```ts
function isNumber<T>(value): boolean;
```

Defined in: [number-utils.ts:20](https://github.com/kemotx90/ts-utily/blob/560ae9f8573563ca541d9fb01f012e8591e5b7e5/src/utils/number-utils.ts#L20)

The function `isNumber` checks if a value is a number in TypeScript.

## Type Parameters

### T

`T`

## Parameters

### value

`T`

The `value` parameter is the value that you want to check if it is a number. It can be of any type `T`.

## Returns

`boolean`

a boolean value.
