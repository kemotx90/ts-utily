```ts
function fileIs(filename, mimeType): boolean;
```

Defined in: [file-utils.ts:111](https://github.com/kemotx90/ts-utily/blob/560ae9f8573563ca541d9fb01f012e8591e5b7e5/src/utils/file-utils.ts#L111)

Checks if a file matches the specified filename and mimeType.

## Parameters

### filename

`string`

The name of the file.

### mimeType

`MimeTypeEnum`

The MIME type to match.

## Returns

`boolean`

Returns `true` if the filename matches the MIME type, otherwise `false`.
