```ts
function toNumber<T>(value): number;
```

Defined in: [number-utils.ts:10](https://github.com/kemotx90/ts-utily/blob/560ae9f8573563ca541d9fb01f012e8591e5b7e5/src/utils/number-utils.ts#L10)

The `toNumber` function converts a value to a number, returning 0 if the value is not a valid number.

## Type Parameters

### T

`T`

## Parameters

### value

`T`

The `value` parameter is of type `T`, which means it can be any type. It represents the value that needs to be converted to a number.

## Returns

`number`

a number.
